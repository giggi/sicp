; ==========
; 0.
; ==========
(define (+ a b)
  (if (= a 0)
      b
      (inc (+ (dec a) b))))

; (+ 4 5)
;   (inc (+ 3 5) b)
;     (inc (+ 2 5) b)
;       (inc (+ 1 5) b)
;         (inc (+ 0 5) b)
;         5
;       6
;     7
;   8
; 9
;
; => 再帰的
;


; ==========
; 1.
; ==========
(define (+ a b)
  (if (= a 0)
      b
      (+ (dec a) (inc b))))

; (+ 4 5)
;   (+ 3 6)
;     (+ 2 7)
;       (+ 1 8)
;         (+ 0 9)
;         9
;       9
;     9
;   9
; 9
;
; => 反復的
;